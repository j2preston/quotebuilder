name: Deploy to Preprod

on:
  push:
    branches: [master]
  workflow_dispatch:

concurrency:
  group: deploy-preprod
  cancel-in-progress: false

env:
  RG:           rg-quotebot-prod
  ACR:          quotebotmqpcv63d.azurecr.io
  API_IMAGE:    quotebotmqpcv63d.azurecr.io/quotebot-api
  WEB_IMAGE:    quotebotmqpcv63d.azurecr.io/quotebot-web
  VITE_API_URL: https://ca-quotebot-api-preprod.thankfuldune-b686df08.uksouth.azurecontainerapps.io

jobs:
  deploy:
    name: Build, push, deploy (preprod)
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Azure login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Login to ACR
        run: az acr login --name quotebotmqpcv63d

      - name: Build and push API image
        run: |
          docker build \
            --file apps/api/Dockerfile \
            --tag ${{ env.API_IMAGE }}:preprod-${{ github.sha }} \
            .
          docker push ${{ env.API_IMAGE }}:preprod-${{ github.sha }}

      - name: Build and push Web image
        run: |
          docker build \
            --file apps/web/Dockerfile \
            --build-arg VITE_API_URL="${{ env.VITE_API_URL }}" \
            --tag ${{ env.WEB_IMAGE }}:preprod-${{ github.sha }} \
            .
          docker push ${{ env.WEB_IMAGE }}:preprod-${{ github.sha }}

      - name: Deploy via Bicep
        run: |
          az deployment group create \
            --resource-group ${{ env.RG }} \
            --template-file infra/main.bicep \
            --parameters @infra/environments/preprod.parameters.json \
            --parameters \
              apiImage="${{ env.API_IMAGE }}:preprod-${{ github.sha }}" \
              webImage="${{ env.WEB_IMAGE }}:preprod-${{ github.sha }}" \
              databaseUrl="${{ secrets.PREPROD_DATABASE_URL }}" \
              redisUrl="${{ secrets.PREPROD_REDIS_URL }}" \
              jwtSecret="${{ secrets.PREPROD_JWT_SECRET }}" \
              anthropicApiKey="${{ secrets.ANTHROPIC_API_KEY }}" \
              twilioAccountSid="${{ secrets.PREPROD_TWILIO_ACCOUNT_SID }}" \
              twilioAuthToken="${{ secrets.PREPROD_TWILIO_AUTH_TOKEN }}" \
              stripeSecretKey="${{ secrets.PREPROD_STRIPE_SECRET_KEY }}" \
              stripeWebhookSecret="${{ secrets.PREPROD_STRIPE_WEBHOOK_SECRET }}" \
              azureStorageConnectionString="${{ secrets.AZURE_STORAGE_CONNECTION_STRING }}"

      - name: Print deployment URLs
        run: |
          API=$(az containerapp show -n ca-quotebot-api-preprod -g ${{ env.RG }} \
            --query "properties.configuration.ingress.fqdn" -o tsv)
          WEB=$(az containerapp show -n ca-quotebot-web-preprod -g ${{ env.RG }} \
            --query "properties.configuration.ingress.fqdn" -o tsv)
          echo "### Preprod deployment complete" >> "$GITHUB_STEP_SUMMARY"
          echo "| | URL |" >> "$GITHUB_STEP_SUMMARY"
          echo "|---|---|" >> "$GITHUB_STEP_SUMMARY"
          echo "| Web | https://${WEB} |" >> "$GITHUB_STEP_SUMMARY"
          echo "| API | https://${API} |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Twilio webhook | https://${API}/api/webhooks/whatsapp |" >> "$GITHUB_STEP_SUMMARY"
